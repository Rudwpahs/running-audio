name: SX1280 FLRC build

on:
  pull_request:
  workflow_dispatch:

jobs:
  firmware:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [tx, rx]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install platformio
      - run: pio run -e ${{ matrix.environment }}

  host-and-protocol:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -r host/requirements.txt
      - run: python -m py_compile host/pr1_wire.py host/pc_audio_sender.py
      - run: python -m unittest discover -s tests -p "test_*.py" -v
      - run: g++ -std=c++17 -Wall -Wextra -Werror -pedantic -Iinclude tests/protocol_test.cpp -o /tmp/pr1_protocol_test
      - run: /tmp/pr1_protocol_test
      - run: g++ -std=c++17 -Wall -Wextra -Werror -pedantic -Iinclude tests/e2e_pipeline_test.cpp -o /tmp/pr1_e2e_test
      - run: /tmp/pr1_e2e_test
